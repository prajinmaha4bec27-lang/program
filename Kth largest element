def findKthLargestSort(nums: list[int], k: int) -> int:
    
    nums.sort(reverse=True)
    
  
    return nums[k - 1]

n
nums = [3, 2, 1, 5, 6, 4]
k = 2
result = findKthLargestSort(nums, k)
print(f"The {k}nd largest element is: {result}")
